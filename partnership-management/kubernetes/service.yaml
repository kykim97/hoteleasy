apiVersion: v1
kind: Service
metadata:
  name: partnership-management
  labels:
    app: partnership-management
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: partnership-management