apiVersion: v1
kind: Service
metadata:
  name: financial-management
  labels:
    app: financial-management
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: financial-management